[misc]
# remote_topgrades = [ "minibox" ]
ignore_failures = [ "remotes", "git" ]
disable = ["pip3"]

[pre_commands]

[post_commands]

# Custom commands
[commands]
"Set it up" = "~/dotfiles/setup.sh"

[git]
repos = [
    "~/dotfiles"
]
