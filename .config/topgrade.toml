[misc]
# remote_topgrades = [ "minibox" ]
ignore_failures = [ "remotes" ]

[pre_commands]

[post_commands]

# Custom commands
[commands]
"Set it up" = "~/dotfiles/setup.sh"

[git]
repos = [
    "~/dotfiles"
]
